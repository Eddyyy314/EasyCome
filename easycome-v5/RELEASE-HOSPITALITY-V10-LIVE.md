# Easy Come Hospitality V10 — Live Preview

Release focused on two corrections:

- preview inside configurator is a persistent interactive app; configuring the parent page no longer reloads it automatically;
- preview navigation updates the app without rebuilding the entire shell or changing scroll position;
- homepage product mock uses a fixed 1180×720 virtual viewport and scales proportionally, so it cannot stretch or squash;
- homepage copy reflects the 12 standard PMS sections introduced in V9.
