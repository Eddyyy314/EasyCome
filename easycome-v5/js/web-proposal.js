(()=>{'use strict';const $=s=>document.querySelector(s);const q=new URLSearchParams(location.search);const demoSlug=q.get('d')||'';const token=q.get('t')||'';let proposal=null;const money=n=>new Intl.NumberFormat('it-IT',{style:'currency',currency:'EUR',maximumFractionDigits:0}).format(Number(n)||0);
async function load(){try{const r=await fetch(`/api/web-proposal-public?d=${encodeURIComponent(demoSlug)}&t=${encodeURIComponent(token)}`,{cache:'no-store'});const d=await r.json();if(!r.ok)throw new Error(d.error||'Proposta non disponibile');proposal=d.proposal;$('#companyName').textContent=`${proposal.companyName}, questo sito è stato pensato per voi.`;$('#projectStatus').textContent=proposal.status==='paid'?'ACQUISTATO':'PRONTO DA VEDERE';$('#projectPrice').textContent=money(proposal.price);$('#priceLarge').textContent=money(proposal.price);if(proposal.previewUrl){$('#sitePreview').src=proposal.previewUrl;$('#openPreview').href=proposal.previewUrl;try{$('#previewHost').textContent=new URL(proposal.previewUrl).hostname}catch{}$('#sitePreview').onload=()=>{$('#previewState').style.display='none';$('#sitePreview').style.display='block'}}else{$('#previewState').innerHTML='<p>Anteprima live non ancora collegata. Contatta Easy Come.</p>';$('#sitePreview').style.display='none';$('#openPreview').style.display='none'}if(proposal.status==='paid'){showPaid()}else if(q.get('paid')==='1'){confirmPayment()}}catch(e){$('#previewState').innerHTML=`<p>${e.message}</p>`;$('#checkoutForm').style.display='none'}}

async function confirmPayment(){const msg=$('#checkoutMessage');msg.textContent='Pagamento inviato. Sto aspettando la conferma sicura di Stripe…';$('#checkoutButton').disabled=true;for(let i=0;i<8;i++){await new Promise(r=>setTimeout(r,1500));try{const r=await fetch(`/api/web-proposal-public?d=${encodeURIComponent(demoSlug)}&t=${encodeURIComponent(token)}&cb=${Date.now()}`,{cache:'no-store'});const d=await r.json();if(r.ok&&d.proposal?.status==='paid'){proposal=d.proposal;showPaid();return}}catch{}}msg.textContent='Pagamento ricevuto da Stripe, conferma Easy Come ancora in elaborazione. Puoi ricaricare questa pagina tra pochi secondi.'}
function showPaid(){$('#paidPanel').hidden=false;$('#checkoutForm').style.display='none';$('#projectStatus').textContent='ACQUISTATO';$('#downloadPackage').href=`/api/web-package-upload?mode=download&d=${encodeURIComponent(demoSlug)}&t=${encodeURIComponent(token)}`;if(proposal?.previewUrl){$('#openPurchasedSite').href=proposal.previewUrl}else{$('#openPurchasedSite').style.display='none'}setTimeout(()=>$('#paidPanel').scrollIntoView({behavior:'smooth',block:'center'}),250)}
$('#checkoutForm').addEventListener('submit',async e=>{e.preventDefault();const b=$('#checkoutButton');const msg=$('#checkoutMessage');msg.textContent='';b.disabled=true;b.textContent='PREPARO IL CHECKOUT…';try{const r=await fetch('/api/web-proposal-checkout',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({demoSlug,token,name:$('#buyerName').value.trim(),email:$('#buyerEmail').value.trim(),phone:$('#buyerPhone').value.trim(),termsAccepted:$('#termsAccepted').checked})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Checkout non disponibile');location.href=d.url}catch(err){msg.textContent=err.message;b.disabled=false;b.textContent='VOGLIO QUESTO SITO →'}});load()})();
