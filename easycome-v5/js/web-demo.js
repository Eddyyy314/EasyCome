(function(){'use strict';const $=(s,r=document)=>r.querySelector(s);const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));const slug=new URLSearchParams(location.search).get('d')||'';function initials(name){return String(name||'EC').trim().split(/\s+/).filter(Boolean).slice(0,2).map(x=>x[0]).join('').toUpperCase()}function fail(m){$('#webApp').className='web-loading';$('#webApp').innerHTML=`<div><b>Anteprima non disponibile</b><p>${esc(m)}</p><a href="/">Easy Come →</a></div>`}function render(p){const b=p.business,h=p.hero,c=p.colors,first=(b.name.split(/\s+/)[0]||b.name).toUpperCase();document.documentElement.style.setProperty('--brand',c.primary);document.documentElement.style.setProperty('--ink',c.ink);document.documentElement.style.setProperty('--paper',c.paper);document.documentElement.style.setProperty('--soft',c.soft);document.documentElement.style.setProperty('--contrast',c.contrast);document.title=`Anteprima sito — ${b.name}`;$('#webApp').className='';$('#webApp').innerHTML=`<div class="wf-bar"><div class="wf-brand"><b>EC</b><div><span>EASY COME WEB FACTORY</span><small>ANTEPRIMA PREPARATA PER ${esc(b.name).toUpperCase()}</small></div></div><div class="wf-actions"><button data-view="desktop">DESKTOP</button><button data-view="mobile">MOBILE</button><a href="/" target="_top">EASY COME</a></div></div><div class="wf-stage" id="stage"><div class="preview-site"><header class="site-head"><div class="site-brand"><span class="site-mark">${esc(initials(b.name))}</span><div><strong>${esc(b.name)}</strong><small>${esc(b.category)}</small></div></div><nav class="site-nav"><a href="#servizi">Servizi</a><a href="#metodo">Come funziona</a><a href="#contatti">Contatti</a><a class="cta-mini" href="#contatti">${esc(p.cta.label)}</a></nav></header><main><section class="web-hero"><div><span class="kick">${esc(h.eyebrow)}</span><h1>${esc(h.headline)}</h1><p>${esc(h.intro)}</p><div class="hero-acts"><a class="main" href="#contatti">${esc(p.cta.label)} →</a><a class="sub" href="#servizi">Scopri i servizi</a></div></div><div class="signature" aria-hidden="true"><span>01 / ${esc(p.family.toUpperCase())}</span><b>${esc(first)}</b><i></i><small>${esc(b.category)}</small></div></section><section class="fact-strip"><div><small>ATTIVITÀ</small><strong>${esc(b.category)}</strong></div><div><small>DOVE</small><strong>${esc(b.address||'Informazioni su richiesta')}</strong></div><div><small>CONTATTO</small><strong>${esc(b.phone||b.email||'Richiesta online')}</strong></div></section><section class="web-section" id="servizi"><div class="section-title"><span class="kick">COSA TROVI QUI</span><h2>Le informazioni giuste, senza farti cercare.</h2></div><div class="cards">${p.services.slice(0,3).map((s,i)=>`<article><span>${String(i+1).padStart(2,'0')}</span><h3>${esc(s.title)}</h3><p>${esc(s.text)}</p><a href="#contatti">Scopri di più →</a></article>`).join('')}</div></section><section class="manifest" id="metodo"><div><span class="kick">IL METODO</span><h2>${p.method.map(esc).join('.<br>')}.</h2></div><p>Una presenza digitale che accompagna il lavoro reale dell’attività e può collegarsi direttamente al Gestionale Easy Come.</p></section><section class="web-close" id="contatti"><span class="kick">CONTATTO DIRETTO</span><h2>${esc(p.cta.label)}.</h2><p>${esc(b.address||'')}</p>${b.phone?`<a href="tel:${esc(b.phone.replace(/\s+/g,''))}">Chiama ${esc(b.phone)} →</a>`:`<a href="#">Invia una richiesta →</a>`}</section></main><footer class="site-foot"><div class="foot-grid"><div><h3>${esc(b.name)}</h3><p>Un sito coordinato con il gestionale, le automazioni e il resto dell’ecosistema Easy Come.</p></div><div><small>CONTATTI</small>${b.phone?`<a href="tel:${esc(b.phone.replace(/\s+/g,''))}">${esc(b.phone)}</a>`:''}${b.email?`<a href="mailto:${esc(b.email)}">${esc(b.email)}</a>`:''}<span>${esc(b.address||'')}</span></div><div><small>SITO</small><a href="#servizi">Servizi</a><a href="#metodo">Metodo</a><a href="#contatti">Contatti</a></div></div><div class="foot-bottom"><span>ANTEPRIMA WEB FACTORY</span><span>Easy Come · Automazione PMI</span></div></footer></div></div>`;document.querySelectorAll('[data-view]').forEach(btn=>btn.onclick=()=>$('#stage').classList.toggle('mobile',btn.dataset.view==='mobile'))}
async function init(){try{if(!slug)throw new Error('Link incompleto.');const r=await fetch(`/api/demo-public?slug=${encodeURIComponent(slug)}`,{cache:'no-store'});const d=await r.json();if(!r.ok)throw new Error(d.error||'Anteprima non disponibile.');render(d.websiteProfile)}catch(e){fail(e.message)}}init();})();
