(function(){'use strict';const $=s=>document.querySelector(s);const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));const slug=new URLSearchParams(location.search).get('d')||'';async function event(name){fetch('/api/demo-event',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({slug,event:name})}).catch(()=>{})}
function fail(m){$('#app').className='';$('#app').innerHTML=`<section class="error"><span style="font-weight:900;color:#ff6b35">EASY COME DEMO</span><h1>Questa anteprima non è disponibile.</h1><p>${esc(m)}</p><a href="index.html" style="font-weight:900">Vai a Easy Come →</a></section>`}
async function init(){try{if(!slug)throw new Error('Link demo incompleto.');const r=await fetch(`/api/demo-public?slug=${encodeURIComponent(slug)}`,{cache:'no-store'});const d=await r.json();if(!r.ok)throw new Error(d.error||'Demo non disponibile.');const {place,model}=d;document.documentElement.style.setProperty('--brand',model.color||'#275dff');document.documentElement.style.setProperty('--accent',model.accent||'#17213b');document.title=`Demo Easy Come — ${place.name}`;const exp=d.expiresAt?new Date(d.expiresAt):null;const expiry=exp?`DEMO FINO AL ${exp.toLocaleDateString('it-IT',{day:'2-digit',month:'short'}).toUpperCase()}`:'DEMO GRATUITA';const studio=`studio.html?demo=${encodeURIComponent(slug)}`;
$('#app').className='';$('#app').innerHTML=`<div class="demo"><aside class="side"><div class="brand"><small>Gestionale demo per</small><strong>${esc(place.name)}</strong></div><nav class="nav">${model.nav.map((x,i)=>`<button class="${i===0?'active':''}">${['▦','◎','◌','▣','◈','◫'][i]||'•'}&nbsp;&nbsp;${esc(x)}</button>`).join('')}</nav><div class="foot"><b style="color:#fff">Easy Come Demo</b><br>Questa anteprima usa esclusivamente dati operativi fittizi. Nome e dati dell’attività sono richiamati live per personalizzare l’esperienza.</div></aside><main class="main"><header class="top"><div><h1>Dashboard — ${esc(place.name)}</h1><p>${esc(place.category)}${place.address?' · '+esc(place.address):''}</p></div><span class="expiry">${expiry}</span></header><section class="kpis">${model.kpis.map((x,i)=>`<article class="kpi"><span>${esc(x)}</span><strong>${esc(model.values[i])}</strong><em>${i===0?'+12% vs ieri':'Aggiornato ora'}</em></article>`).join('')}</section><section class="content"><article class="card"><div class="card-head"><h2>Operatività recente</h2><span>LIVE DEMO</span></div><div style="overflow:auto"><table class="table"><thead><tr><th>Cliente</th><th>Dettaglio</th><th>Info</th><th>Stato</th></tr></thead><tbody>${model.rows.map(r=>`<tr><td><b>${esc(r[0])}</b></td><td>${esc(r[1])}</td><td>${esc(r[2])}</td><td><span class="status">${esc(r[3])}</span></td></tr>`).join('')}</tbody></table></div></article><article class="card"><div class="card-head"><h2>Automazioni recenti</h2><span>ATTIVE</span></div><div class="events">${model.activity.map((x,i)=>`<div class="event"><i></i><div><b>${esc(x)}</b><span>${['2 minuti fa','18 minuti fa','1 ora fa'][i]||'oggi'}</span></div></div>`).join('')}</div></article></section><section class="cta"><div><h2>Vuoi trasformare questa demo nel tuo gestionale?</h2><p>Apri Easy Come Studio: troverai già caricata questa configurazione e potrai modificarla prima di qualsiasi acquisto.</p></div><a id="cta" href="${studio}">Personalizzalo →</a></section><div class="meta"><span>Anteprima Easy Come · tutti i clienti, importi e attività nel gestionale sono dati dimostrativi.</span><span class="google" translate="no">Google Maps</span></div></main></div>`;$('#cta').onclick=()=>event('cta');document.querySelectorAll('.nav button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.nav button').forEach(x=>x.classList.remove('active'));b.classList.add('active')});}catch(e){fail(e.message)}}init();})();
